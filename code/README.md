# Code to replicate the results
1. combined_apex_mic_new.py : Helper function that takes in a list of peptides and runs APEX model inference and produces the regressed MIC values as a dataframe
2. plt_peptides_1x4grid.py : Helper function that takes in a list of peptides, runs 3D strcuture extraction and plots them
3. zampa_paper.ipynb : A colab notebook to generate the data assets and plots used in the paper

